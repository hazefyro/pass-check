import { bench, describe } from 'vitest'
// TODO FINISH BENCHES
describe('generator performance', () => {
  describe('standard', () => {
    bench('random password', () => {})
    bench('word password', () => {})
  })
  describe('maxed', () => {
    bench('random password', () => {})
    bench('word password', () => {})
  })
})
